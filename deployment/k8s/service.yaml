apiVersion: v1
kind: Service
metadata:
  name: scania-aps-failure-api
spec:
  type: NodePort
  selector:
    app: scania-aps-failure-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30081
